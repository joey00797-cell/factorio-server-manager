# Factorio Server Manager — Docker Image

## Setup & Run

```bash
docker run -it --rm \
  -v /opt/fsm-data:/opt/fsm-data \
  ghcr.io/joey00797-cell/fsm:latest
```

Follow the wizard — it will configure everything and generate a start script.
